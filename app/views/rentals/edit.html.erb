<h1>Edit Rental Info</h1>
<%= render partial: "form" %>
  <% if can? :destroy, @rental %>
<%= form_for [@user, @rental], method: :delete do |f| %>
  <%= f.submit "Fulfilled!", data: { confirm: 'Are you sure?' } %>
    <% end %>
<% end %>
